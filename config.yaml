# =============================================================================
# Runner Configuration
# =============================================================================
# runner: goose                    # Runner type (currently only "goose" supported)
# goose-bin: /path/to/goose        # Default goose binary (used if goose-bins not specified)
#
# goose-bins:                      # Binaries to test - each agent runs against ALL binaries
#   - name: release
#     path: /Users/micn/Documents/code/goose/target/release/goose
#   - name: default
#     path: goose

runner: goose
goose-bins:
  - name: release
    path: /Users/micn/Documents/code/goose/target/release/goose
  - name: default
    path: goose

# =============================================================================
# Agent Configurations
# =============================================================================
agent-config:
  # --- Anthropic models (commented out for now) ---
  # - name: opus-baseline
  #   provider: anthropic
  #   model: claude-opus-4-5-20251101
  #   extensions: [developer, todo, skills, code_execution, extensionmanager]

  # - name: opus-full
  #   provider: anthropic
  #   model: claude-opus-4-5-20251101
  #   extensions: [developer, todo, skills, code_execution, extensionmanager]
  #   stdio:
  #     - node mcp-harness/dist/index.js

  # --- Ollama models (commented out) ---
  # - name: nemotron-full
  #   provider: ollama
  #   model: nemotron-3-nano:latest
  #   extensions: [developer, todo, skills, code_execution, extensionmanager]
  #   stdio:
  #     - node mcp-harness/dist/index.js

  # --- ACTIVE: Models to test ---
  - name: kimi-k2.5-full
    provider: ollama
    model: kimi-k2.5:cloud
    extensions: [developer, todo, skills, code_execution, extensionmanager]
    stdio:
      - node mcp-harness/dist/index.js

  - name: gpt-oss-120b-full
    provider: ollama
    model: gpt-oss:120b-cloud
    extensions: [developer, todo, skills, code_execution, extensionmanager]
    stdio:
      - node mcp-harness/dist/index.js

  - name: gpt-oss-20b-full
    provider: ollama
    model: gpt-oss:20b
    extensions: [developer, todo, skills, code_execution, extensionmanager]
    stdio:
      - node mcp-harness/dist/index.js

  - name: qwen3-coder-64k-full
    provider: ollama
    model: qwen3-coder:64k
    extensions: [developer, todo, skills, code_execution, extensionmanager]
    stdio:
      - node mcp-harness/dist/index.js

  # --- More Ollama models (commented out) ---
  # - name: qwen3-coder-64k-lean-code-mode
  #   provider: ollama
  #   model: qwen3-coder:64k
  #   extensions: [developer, code_execution]

  # - name: qwen3-coder-64k-lean
  #   provider: ollama
  #   model: qwen3-coder:64k
  #   extensions: [developer]

  # - name: glm-4.7-flash-full
  #   provider: ollama
  #   model: glm-4.7-flash:latest
  #   extensions: [developer, todo, skills, code_execution, extensionmanager]
  #   stdio:
  #     - node mcp-harness/dist/index.js

  # - name: glm-4.7-flash-lean
  #   provider: ollama
  #   model: glm-4.7-flash:latest
  #   extensions: [developer]

  # - name: glm-4.7-flash-lean-code-mode
  #   provider: ollama
  #   model: glm-4.7-flash:latest
  #   extensions: [developer, code_execution]

# =============================================================================
# Test Matrix
# =============================================================================
# Which scenarios run with which agents
# - Omit 'agents' to run against ALL active agents
# - Specify 'agents' list to restrict to specific agents

matrix:
  # simple-file-create runs against ALL active agents
  - scenario: simple-file-create

  # everyday-app-automation runs against agents with stdio (mcp-harness)
  - scenario: everyday-app-automation
    agents:
      [
        kimi-k2.5-full,
        gpt-oss-120b-full,
        gpt-oss-20b-full,
        qwen3-coder-64k-full,
      ]
